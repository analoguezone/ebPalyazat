version: "3"
services:
  app:
    build: .
    restart: always
    environment:
      - EMAIL_PASSWORD=${EMAIL_PASSWORD}
      - PORT=4150
    volumes:
      - docroot:/usr/src/app

  proxy:
    build: proxy
    restart: always
    ports:
      - "5650:5650"
    links:
      - app
    volumes:
      - docroot:/usr/src/app

volumes:
  docroot:
